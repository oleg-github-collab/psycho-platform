# Психологічна Платформа

Потужна платформа для психологічних груп з повним функціоналом для спілкування, вебінарів та управління зустрічами.

## 🚀 Функціонал

### Основні можливості:
- ✅ **Автентифікація** - проста реєстрація з логіном та паролем
- ✅ **Теми для обговорення** - публічні та приватні теми з голосуванням
- ✅ **Месенджер** - реальний час через WebSocket з реакціями, цитатами, групами
- ✅ **Групи** - публічні та приватні групи для спілкування
- ✅ **Вебінари** - інтеграція з 100ms для проведення онлайн-зустрічей
- ✅ **Календар** - система запису на консультації
- ✅ **Адмін-панель** - потужне управління користувачами та контентом
- ✅ **Mobile-first дизайн** - адаптивний glassmorphism UI

### Для психологів:
- Створення та проведення вебінарів
- Управління записами клієнтів
- Приватні групи для терапії
- Календар зустрічей

## 🛠 Технології

**Backend:**
- Go 1.21
- Gin Web Framework
- PostgreSQL
- Redis
- WebSocket (gorilla/websocket)
- JWTAuthentications
- 100ms API для стрімінгу

**Frontend:**
- Vanilla JavaScript (ES6+)
- Glassmorphism CSS
- Mobile-first responsive design

## 📦 Встановлення

### Локальна розробка

1. Клонуйте репозиторій:
```bash
git clone <your-repo-url>
cd Платформа
```

2. Встановіть залежності:
```bash
go mod download
```

3. Створіть файл `.env`:
```bash
cp .env.example .env
```

4. Налаштуйте змінні оточення:
```env
DATABASE_URL=postgres://user:password@localhost:5432/psycho_platform?sslmode=disable
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-key-change-in-production
HMS_API_KEY=your-100ms-api-key
HMS_API_SECRET=your-100ms-api-secret
ENVIRONMENT=development
FRONTEND_URL=http://localhost:3000
PORT=8080
```

5. Запустіть PostgreSQL та Redis:
```bash
# PostgreSQL
docker run -d --name psycho-postgres -e POSTGRES_DB=psycho_platform -e POSTGRES_PASSWORD=password -p 5432:5432 postgres:15

# Redis
docker run -d --name psycho-redis -p 6379:6379 redis:7
```

6. Запустіть сервер:
```bash
go run cmd/api/main.go
```

Відкрийте http://localhost:8080

## 🚀 Розгортання на Railway

### Автоматичне розгортання

1. Створіть новий проект на [Railway](https://railway.app)

2. Підключіть GitHub репозиторій

3. Додайте PostgreSQL:
   - Натисніть "New" → "Database" → "PostgreSQL"

4. Додайте Redis:
   - Натисніть "New" → "Database" → "Redis"

5. Налаштуйте змінні оточення в Railway:
```
DATABASE_URL=${DATABASE_URL}
REDIS_URL=${REDIS_URL}
JWT_SECRET=<generate-random-secret>
HMS_API_KEY=<your-100ms-key>
HMS_API_SECRET=<your-100ms-secret>
ENVIRONMENT=production
FRONTEND_URL=https://your-app.railway.app
```

6. Railway автоматично розгорне додаток!

### Використання Railway CLI

```bash
# Встановіть Railway CLI
npm i -g @railway/cli

# Увійдіть
railway login

# Ініціалізуйте проект
railway init

# Додайте PostgreSQL
railway add --plugin postgresql

# Додайте Redis
railway add --plugin redis

# Розгорніть
railway up
```

## 📚 API Endpoints

### Auth
- `POST /api/auth/register` - Реєстрація
- `POST /api/auth/login` - Вхід
- `GET /api/auth/me` - Поточний користувач

### Topics
- `GET /api/topics` - Список тем
- `POST /api/topics` - Створити тему
- `POST /api/topics/:id/vote` - Голосувати

### Messages
- `GET /api/messages` - Список повідомлень
- `POST /api/messages` - Створити повідомлення
- `POST /api/messages/:id/reactions` - Додати реакцію

### Groups
- `GET /api/groups` - Список груп
- `POST /api/groups` - Створити групу
- `POST /api/groups/:id/join` - Приєднатись
- `POST /api/groups/:id/leave` - Вийти

### Sessions (Webinars)
- `GET /api/sessions` - Список сесій
- `POST /api/sessions` - Створити сесію
- `GET /api/sessions/:id/token` - Отримати токен для 100ms

### Appointments
- `GET /api/appointments` - Мої зустрічі
- `POST /api/appointments` - Записатись
- `PATCH /api/appointments/:id/status` - Оновити статус

### Admin
- `GET /api/admin/stats` - Статистика
- `GET /api/admin/users` - Список користувачів
- `PATCH /api/admin/users/:id/status` - Активувати/деактивувати
- `PATCH /api/admin/users/:id/psychologist` - Встановити психолога

### WebSocket
- `GET /api/ws` - WebSocket підключення

## 🎨 Дизайн

Платформа використовує сучасний **glassmorphism** дизайн з:
- Напівпрозорі елементи з blur ефектом
- Градієнтні фони
- Smooth анімації
- Mobile-first підхід
- Адаптивна верстка

## 🔒 Безпека

- JWT токени для автентифікації
- Bcrypt для хешування паролів
- CORS захист
- Валідація даних
- Роль-базований доступ

## 📱 Функціонал для мобільних

- Responsive дизайн
- Touch-friendly інтерфейс
- Оптимізація для маленьких екранів
- PWA ready (можна додати manifest.json)

## 🎯 Roadmap

- [ ] Push-повідомлення
- [ ] Файлові вкладення
- [ ] Пошук по повідомленням
- [ ] Експорт даних
- [ ] Email notifications
- [ ] PWA підтримка

## 📄 Ліцензія

MIT License

## 👥 Підтримка

Для питань та підтримки створіть issue в GitHub репозиторії.
